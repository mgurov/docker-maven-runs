<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
     Simple demo project demonstrating the usage of rhuss/docker-maven-plugin
  -->
  <groupId>com.github.mgurov</groupId>
  <artifactId>docker-maven-runs</artifactId>
  <version>0.0.1</version>
  <packaging>pom</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.jolokia</groupId>
        <artifactId>docker-maven-plugin</artifactId>
        <version>0.13.4-SNAPSHOT</version>
        <configuration>
          <images>

            <image>
              <name>local/bes</name>
              <build>
                <entryPoint>
                  <shell>echo build entrypoint shell</shell>
                </entryPoint>
              </build>
              <run>
                <log>
                  <prefix>BES</prefix>
                </log>
                <wait>
                  <log>build entrypoint shell</log>
                </wait>
              </run>
            </image>

            <image>
              <name>local/bee</name>
              <build>
                <entryPoint>
                  <exec>
                    <arg>echo</arg>
                    <arg>build</arg>
                    <arg>entrypoint</arg>
                    <arg>exec</arg>
                  </exec>
                </entryPoint>
              </build>
              <run>
                <log>
                  <prefix>BEE</prefix>
                </log>
                <wait>
                  <log>build entrypoint exec</log>
                </wait>
              </run>
            </image>

            <image>
              <name>local/bcs</name>
              <build>
                <cmd>
                  <shell>echo build cmd shell</shell>
                </cmd>
              </build>
              <run>
                <log>
                  <prefix>BCS</prefix>
                </log>
                <wait>
                  <log>build cmd shell</log>
                </wait>
              </run>
            </image>

            <image>
              <name>local/bce</name>
              <build>
                <cmd>
                  <exec>
                    <arg>echo</arg>
                    <arg>build</arg>
                    <arg>cmd</arg>
                    <arg>exec</arg>
                  </exec>
                </cmd>
              </build>
              <run>
                <log>
                  <prefix>BCE</prefix>
                </log>
                <wait>
                  <log>build cmd exec</log>
                </wait>
              </run>
            </image>

            <image>
              <name>busybox</name>
              <run>
                <entrypoint>echo run entrypoint shell inline</entrypoint>
                <log>
                  <prefix>RESI</prefix>
                </log>
                <wait>
                  <log>run entrypoint shell inline</log>
                </wait>
              </run>
            </image>


            <image>
              <name>busybox</name>
              <run>
                <cmd>echo run cmd shell inline</cmd>
                <log>
                  <prefix>RCSI</prefix>
                </log>
                <wait>
                  <log>run cmd shell inline</log>
                </wait>
              </run>
            </image>

<!--
ok, but problems with stopping most of the time.
            <image>
              <name>busybox</name>
              <run>
                <entrypoint></entrypoint>
                <cmd></cmd>
                <log>
                  <prefix>empty_cmd</prefix>
                </log>
              </run>
            </image>
-->
<!--
  fails as either shell or command

            <image>
              <name>local/shellandexec</name>
              <build>
                <entryPoint>
                  <shell>echo build entrypoint shell</shell>
                  <exec>
                    <arg>shellandexec</arg>
                    <arg>b</arg>
                  </exec>
                </entryPoint>
              </build>
              <run>
                <log>
                  <prefix>shellandexec</prefix>
                </log>
                <wait>
                  <log>shellandexec</log>
                </wait>
              </run>
            </image>
-->
<!--
            either shell or cmd, but happens after the build already has started
            <image>
              <name>local/emptyshell</name>
              <build>
                <entryPoint>
                </entryPoint>
              </build>
              <run>
                <log>
                  <prefix>emptyshell</prefix>
                </log>
              </run>
            </image>

-->

          </images>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
